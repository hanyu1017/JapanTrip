# Railway Production Environment Variables Example
# Copy these to your Railway project settings

# Database (provided by Railway PostgreSQL plugin)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Server Configuration
PORT=${{PORT}}
NODE_ENV=production

# Frontend URL (Railway will provide this)
CLIENT_URL=${{RAILWAY_STATIC_URL}}
